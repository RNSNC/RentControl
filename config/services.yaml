# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Admin\DocumentAdmin:
        arguments: [ ~, App\Entity\Document,~ ]
        tags:
            - { name: sonata.admin, manager_type: orm, group: Аренда инструментов, label: Документы }

    App\Admin\CounterpartyAdmin:
        arguments: [ ~, App\Entity\Counterparty,~ ]
        tags:
            - { name: sonata.admin, manager_type: orm, group: Аренда инструментов, label: Контрагенты }

    App\Admin\InstrumentGroupAdmin:
        arguments: [ ~, App\Entity\InstrumentGroup,~ ]
        tags:
            - { name: sonata.admin, manager_type: orm, group: Аренда инструментов, label: Названия группы }

    App\Admin\InstrumentNameAdmin:
        arguments: [ ~, App\Entity\InstrumentName,~ ]
        tags:
            - { name: sonata.admin, manager_type: orm, group: Аренда инструментов, label: Названия инструментов }

    App\Admin\PhoneAdmin:
        arguments: [ ~, App\Entity\Phone,~ ]
        tags:
            - { name: sonata.admin, manager_type: orm, group: Аренда инструментов, label: Телефоны }

    App\Admin\PlaceOfUseAdmin:
        arguments: [ ~, App\Entity\PlaceOfUse,~ ]
        tags:
            - { name: sonata.admin, manager_type: orm, group: Аренда инструментов, label: Место дислокации }

    App\Admin\RentAdmin:
        arguments: [ ~, App\Entity\Rent,~ ]
        tags:
            - { name: sonata.admin, manager_type: orm, group: Аренда инструментов, label: Аренда }


    App\Admin\StorageAdmin:
        arguments: [ ~, App\Entity\Storage,~ ]
        tags:
            - { name: sonata.admin, manager_type: orm, group: Аренда инструментов, label: Склад }
